This library implements a 'Defer' statement in c, allowing statements to run at
return time, generally to do cleanup.  It is analogous to 'defer' in Go or
'scope(exit)' in D.  Integrating it into your codebase is simple; see demo.c
for example usage.
